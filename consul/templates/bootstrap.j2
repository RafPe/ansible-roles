{
    "bootstrap_expect": {{consul_bootstrap_expect}},
    "server": {{consul_server | lower}},
    "datacenter": "{{consul_datacenter}}",
{% if consul_ui == true %}  "ui_dir" : " {{consul_ui_dir}}", 
{% endif %} 
    "data_dir": "{{ consul_data_dir }}",
    "log_level": "{{ consul_log_level}}",
    "enable_syslog": {{ consul_enable_syslog | lower }},
    "advertise_addr": "{{consul_bootstrap_primary}}",
    "bind_addr": "{{consul_bootstrap_primary}}",
    "client_addr" : "{{ consul_bootstrap_primary }}"

}
